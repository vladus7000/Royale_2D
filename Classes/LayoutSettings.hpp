#pragma once

#include <string>

namespace LayoutSettings
{
    //Fonts
    const std::string menuFont("fonts/comic.ttf");

    //Images
    const std::string menuBackground("menu_background.png");

    //Const
    const float fontSize = 60.0f;
    const float step = 70.0f;
    const float MainMenuPercentageFromLeft = 0.2f;
    const float MainMenuPercentageFromBottom = 0.75f;
    const float PauseMenuPercentageFromLeft = 0.1f;
    const float PauseMenuPercentageFromBottom = 0.75f;
}
